#### 
```
    1. 需要对方机器开启远程控制
        目标电脑：
            右键我的电脑 -> 属性 -> 远程设置 -> 远程(勾选"远程协助"->"允许远程协助链接这台计算机"，勾选"远程桌面"->"允许远程链接到此计算机") -> 添加 -> 高级 -> 立即查找(在搜索结果中选择指定用户) -> 一直确定

    2. 远程链接
        工具电脑：
            win(开始)+r -> cmd -> 输入mstsc(打开远程桌面链接) -> 显示选项 -> 在"常规"中输入计算机IP+端口号(远程输入公网ip，局域网输入内网ip，默认端口3389)和用户名(默认Administrator管理员账号) -> 链接

```